import { StyleSheet } from "react-native";

const estiloperfil = StyleSheet.create ({
    pagina:{
        flex: 1,
        alignItems: "center",
        marginTop: 20
    }
})

export default estiloperfil